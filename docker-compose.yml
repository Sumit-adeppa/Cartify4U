version: "3.8"

services:
  spring-app:
    build: .
    container_name: spring-app
    ports:
      - "9090:9090"
    environment:
      # Connect to MongoDB running on the host machine
      SPRING_DATA_MONGODB_URI: mongodb://host.docker.internal:27017/Cartify
 
